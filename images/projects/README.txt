Place project images here.

When adding a project via the Admin Panel, upload the image using the
image upload field. Images are stored as base64 in localStorage automatically.

Accepted formats: JPG, PNG, GIF, WebP
